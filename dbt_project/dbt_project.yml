name: 'urg_migration'
version: '1.0.0'
config-version: 2

profile: 'urg_migration'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  urg_migration:
    silver:
      +materialized: table
      +schema: silver
    gold:
      +materialized: table
      +schema: gold